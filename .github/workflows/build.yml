name: Build KivyMD APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Clean stuck cache and Build APK
        run: |
          cd $GITHUB_WORKSPACE
          
          # Remove the specific file that causes the "checking to merge" freeze
          rm -f $HOME/.buildozer/android/platform/android-sdk/.temp
          
          docker run --rm \
            -v $GITHUB_WORKSPACE:/home/user/hostcwd \
            -v $HOME/.buildozer:/home/user/.buildozer \
            kivy/buildozer android debug

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: app-debug
          path: bin/*.apk
